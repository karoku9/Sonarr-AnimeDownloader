# Progress
2026-09-14: Invoked planning-with-files, repo-map, repo-review; inspected V4 Git state and prior specifications. Created persistent planning files; credential scan records path/reason only. No V3 access or mutation.

Regression red verified: missing src.v4. Draft metadata/matcher/review modules prepared in ignored work only; checkpoint awaiting Git author.

User explicitly skipped baseline commit and Git identity configuration. Safe baseline staging retained; installed draft src/v4 modules without runtime registration.

Implemented src/v4 foundation and read-only shadow; 32 V4 tests passed and 54 compatible legacy tests passed. Real shadow: one target, zero local catalog candidates; no meaningful production parity claim. Updated foundation contract and draft links.

Standards pre-review found global alias falsely certifying an unknown target season. Confirmed red with a focused test, fixed scope check, will freeze new review basis and replay all V4 checks.

Final verification: 34 V4 + 54 compatible legacy tests passed (88 total). repo-review final Worktree code/docs scope 43ea905891640c7f66e511856dc60fc594a5793f67fa37895ba8d4991ae22055; no open foundation findings after two reproduced/fixed regressions. Report retained in ignored work/repo-review.md and .json. Graphify navigation query interrupted after delay; AST map refresh completed; no formal repo-map claim. No commit/push/Git config/LLM/frontend/runtime operations.

Phase3: unstaged V4 only, no commit or Git config. 50 V4 +54 compatible legacy tests pass (104 total, previous88 retained). New files provenance.py/dataset.py/meaningful_shadow.py/review_store.py and three test modules. Models/matcher/normalization/contracts updated. Reports and hash review basis in work/phase3 (ignored). Input SHA-256 unchanged. repo-map CAPABILITY_MISSING; no invented graph coverage. Standards/Spec review bounded foundation; real mandatory shadow cohort gap remains.

Final replay correction: documentation_mapping is table in V3 scorer, retaining original table treatment. Final statistics supersede intermediate run: 84 V3 matches, 6 V4, 83 differences, 94 review proposals. Target source hashes unchanged.

Phase4: used planning-with-files. GET-only one-shot container export and927 metadata detail pages, no errors. Sanitized capture/version1 native snapshot/report under work/phase4. Stable12-case real fixture and11 regression tests added. All115 tests pass (61 V4+54 compatible legacy). SHA of matching/models/provenance/normalization unchanged vsPhase3; V3catalog/table/manualalias file hashes unchanged before/after. No V3/Sonarr/Plex/AnimeWorld mutation, cache refresh, mapping/review writes, LLM/UI/downloader/calibration/commit/config. Finishing repo-review Standards/Spec Worktree.

repo-review Phase4 complete: independent Standards/Spec Worktree passes, frozen34-file source basis hashes unchanged; no openP0-P2 within replay scope. Production correctness/CI/whole inheritedfork notverified. Reports under work/phase4/repo-review.md/.json.


Phase5: 192 frozen targets; 128/174 missing crosswalk resolved; 145 auto,47 review; one Black composite; one inferred Crystal shared release, zero verified boundaries. 144 tests green (90+54). Source dates/numbering retained, thresholds unchanged. 24 transport-only/19 other disagreements; Chainsaw date-variant ambiguity is one possible future judge triage. See docs/v4/release-resolver.md and work/phase5/replay. Repo-review pending.

Phase5 repo-review complete: Worktree HEAD e979b64c5a51c0ec9e33ac9c135bd5d641f7846f, branch v4, staged empty. Independent sequential Standards and Spec passes; no open P0–P2. 14 prior source hashes unchanged; exact192 target set and baseline unchanged. Scope offline only; runtime/CI/production inverse-boundary verification not verified. Evidence work/phase5/review-basis.json and repo-review.md. Stop: no LLM/UI/downloader/runtime writes.

Phase6: application orchestrates offline metadata adapter/native candidate rebuild/frozen foundation/local storage. Composite reviews group target IDs atomically (Crystal S1+S2). ApplicationStore extends ReviewRepository DB primitives in separate v1 tables; original and audit immutable via SQL. WSGI standalone /api/v4 DTO API; no web dependency install, mount or server startup. 29 integration tests green after fixing a test-only unclosed sqlite connection. Contract docs/v4/frontend-api-contract.md. Full144 regression suite and repo-review pending.

Phase6 real application scan:192 targets→191 atomic plan units;144 proposed,47 open reviews,0 approved. All network patched forbidden. One foundation retained match without concrete plan becomes application review, not changed matcher behavior. JSON report work/phase6/application-scan-report.json; local DB work/phase6/application-v1.sqlite3. Baseline144 maintained;31 new tests passed,32nd scan-never-approves invariant added; final176 verification and repo-review pending.

Phase6 final:176 tests green (90 foundation V4 +32 API integration +54 legacy,
separate processes). False whole-season date conflict for Black composite
presentation reproduced by failing test and fixed using segment crosswalk evidence;
raw historical evidence remains Advanced. Foundation12 protected Phase5 source
hashes unchanged. Independent Standards/Spec repo-review bounded pass; all29 scope
hashes fixed, staged empty. SQLite read-only integrity/digest/current-scope checks
passed,192target set preserved,0automatic approvals. Frontend contract4.0 complete;
API standalone/unmounted, WSGI live/proxy/operator-authentication not verified.
Future decisions: runtime mounting/same-origin, authenticated actor if needed,
explicit old-review import, async scans and downstream routing guards for approved
inferred plans. No frontend/LLM/downloader enabled. Stop after report.


## Phase 7 started
Recovered Phase6 176-green baseline; no new tests run yet. Git index empty; read-only safe.directory command override avoids shared Git config changes. UI skills now present. Frozen initial src/v4 hashes under work/phase7.


## Phase7 implementation / visual pass1
Runtime4.1 loopback same-origin, durable single-worker job API and local-user audit implemented around unchanged foundation. New ES-module frontend uses API only. All176 prior tests green (122V4+54legacy); new15runtime+12frontend regressions green at this checkpoint. Browser first launch no scan/import, Run scan shows5% progress and creates192-target/191-item replay with144proposed47review. Black destination ranges verified; Crystal26/boundary14/12 human text and required acknowledgement confirmed; rejection has no acknowledgement checkbox. Narrow375px documentWidth360 (15px scrollbar), no app overflow. Contrast tests pass. Fixed redundant single queue, brand wrapping, SUB/DUB vs language, skip-link routing, off-page activity deep links, newest-first activity, fast scan completion refresh and malformed static Host. All UI action tests run on explicit V4-only visual-check SQLite backup, preserving main preview proposal DB.


## Phase7 visual interaction pass2
Browser on isolated V4 visual-check DB: Nadia approve stores reason/local-user; Activity link opens expanded Nadia on page5/8 and visible detail156..719px. Crystal reject without acknowledgement, then Reopen returns open with original uncertainty retained. Chainsaw supported SUB alternative selection updates variant dropdown and stores chosen plan; no unsupported candidate choices. Settings language change blocked navigation until saved; English/Italian persist and render. Five-screen640px effective layout: documentWidth625, zero Advanced open; viewport375 Crystal documentWidth360. In-app Control+plus did not alter zoom (DPR1), so native zoom is explicitly not verified;640px is the effective CSS-layout check equivalent to200% of1280px. No matcher/resolver/normalization/provenance/adapter hash drift; only old application/api/store files changed. Corrected one date-DTO serialization failure with ISO strings, contract regression passes.

Browser keyboard pass found native modal boundary Tab leaving focus at BODY. Added dedicated ES-module Tab/Shift+Tab boundary trap, first4 frontend negative regressions, runtime asset allowlist. Intermediate Tab and Escape remain native; no source/DB side effects.


## Phase7 final implementation and review preparation
2026-09-15: Browser became available after returning current Codex task to foreground. Full V4 server same-origin smoke on temporary127.0.0.1:5904: static assets200, overview144proposed/47openReviews, original5004 binding now blocked by Windows excluded TCP range4926–5025 (netsh read-only evidence); runtime default remains5004 and system ports unchanged. Tab/Shift+Tab modal containment, Escape+focus restore browser-computed. Black two12-episode releases and Crystal26 combined S1/S2 uncertain verified on actual UI. Activity first pass exposed 50 repetitive auto-events; changed public4.1 simple timeline to scan/human/settings events, raw append-only audit retains all events; RED/GREEN regression proves behavior and Nadia human reason/title. Settings double divider fixed by CSS, Library H1→H2 heading hierarchy restored while Review H1→H2→H3 unchanged; region/nav accessible labels localized. Fresh Vercel source fetched for final Web Interface Guidelines review. Browser all5 at effective640 CSS px no document overflow or Advanced open; Crystal375 CSS px documentWidth362; native200% zoom was not achieved and remains Not verified. Final current-source JPEG screenshots stored ignored in work/phase7/screenshots. Main preview V4 DB never used for human action tests; separate visual-check V4 copy was.
Final suites:138 V4 tests,54 compatible legacy,18 Node frontend =210 green. Initial sandbox write restriction caused mkdtemp infinite retry; escalated V4-only test run resolved it. All23 prior src/v4 protected files unchanged except the explicitly authorized API/application/store trio. No Git commit/stage/config, V3 modification, frontend_OLD reuse, LLM, downloader, Sonarr writes or Docker change.

Phase7 final repo-review: Worktree/index basis branch v4 HEAD e979b64c5a51c0ec9e33ac9c135bd5d641f7846f; complete status inventoried (118 porcelain entries), index empty. Bounded standards and originating-user-spec passes sequential/read-only after correcting historical Phase0 status in docs/v4/migration-plan.md. No open P0–P3 in Phase7 frontend/runtime/API/docs/test scope; inherited V3 dirty paths and deployment/live-adapter behavior excluded.15-file critical-source aggregate SHA-256 b56a71bd1395d29fe932ef5a30c22302e8cb7113c5e39bcc16ae8b8644ff04c9 (before planning status update). Browser screenshots and DB ignored under work/phase7; main current-source HTTP overview running144proposed/47review/192targets on5904. Port5004 current binding blocked by Windows reserve, native200% browser zoom not verified. Stop: no LLM, downloader, Sonarr writes or V3 migration.

## Phase8 runtime integration / E2E checkpoint

Changed default runtime and both V4 Compose host ports to6004. Compose now builds only Dockerfile.v4: src/v4, frontend-v4 source assets and stable sanitised12-target production fixture; no src/main.py, downloader, Sonarr environment/API key, V3 mount or old data volume. V4 application SQLite survives in named volume anidown-v4-application; rootfs read-only, user10001, capabilities dropped and no-new-privileges. Explicit Docker bridge transport preserves local Host/Origin checks; normal CLI transport still loopback-only. V4 image built and container healthy127.0.0.1:6004, V3 healthy throughout.

Browser on container: five pages rendered; Run scan showed5% while UI usable, completion updated8 proposals/3 open review from the stable fixture. API smoke POST202→queued/running→completed for12 targets. Crystal review approved with explicit14/12 uncertainty acknowledgement and E2E-labelled reason; Attack on Titan review rejected with E2E-labelled reason. Activity shows both as local-user. Restarted only anidown-v4; original IDs, digest, created_at, decisions and audit match before/after, now8 proposed/1 approved/1 rejected/1 open. No V3/Sonarr request or download.

True Chrome native page zoom200% via isolated V4-only Selenium profile (Chromium partition default zoom level): devicePixelRatio2/visualScale1/CSS client width624, all five routes and resolved Crystal review have document scrollWidth<=clientWidth and Advanced closed. Screenshot ignored work/phase8/native-zoom-200-crystal.png. Existing responsive CSS640/375 checks retained. Full suite139 V4+54 compatible legacy+18 Node frontend=211 green; Compose main/test validate, API smoke and zoom smoke pass. Phase8-delta repo-review pending.

Phase8 final bounded repo-review: Worktree/index basis branch v4 HEAD e979b64c5a51c0ec9e33ac9c135bd5d641f7846f, complete status122 entries, staged empty. Standards and originating-user-Spec passes examined only the12 Phase8 Docker/runtime/port/test/contract files and their existing API/storage closure, aggregate source SHA-256 f2867208bff66e224091e4c8b7d8cb1b54a427cfc025530fe10e4f29749f0b45. No open P0–P3 in this delta; inherited V3 dirty state, live adapter behavior and full-fork release readiness excluded. Current Docker inspection: image anidown-v4:latest, V4-only command, mount /app/work/phase8, readonly rootfs, environment keys contain no Sonarr/API credentials. Container and V3 both healthy; local final URL127.0.0.1:6004. Explicit limitation: Docker scan uses stable sanitized12-target fixture, while larger192-target local offline replay remains a separate CLI configuration. No LLM/UI redesign/downloader/Sonarr writes/V3 migration.
# Phase 9 checkpoint — optional LLM judge

Implemented provider-neutral contract, disabled/mock/Gemini providers, sanitized admissible shortlist, local strict JSON validation, suggestion-only application wiring, V4-local append-only judge attempts and SHA-256 cache. Gemini key absent from development process; no real remote call. Red/green regressions proved `season_conflict` reason independently hard, redacted an URL embedded in an allowlisted field, and bounded the outbound Gemini body before any call. Phase9 has 13 regression tests; final full V4 suite passed 152, earlier legacy54 and frontend18 passed, total224. Optional V4 Compose env passthrough validated with `docker compose --profile v4-preview config --quiet` (exit0; Docker CLI warned that its user config was unreadable); provider/runtime targeted30 passed after Compose change. Bounded Worktree/index repo-review remains. V3 and runtime container untouched.

## Phase 9 final bounded review

Worktree/index basis branch v4 HEAD e979b64c5a51c0ec9e33ac9c135bd5d641f7846f;
complete status125 entries, staged empty. Standards and originating Spec reviewed
the nine-file optional-judge delta and required application/API/storage closure.
No open P0-P3. Aggregate SHA-256
e08ff624cb0ed840eb66d65404c22a7645793e2bfbd670073bf3fedf97374.
Pertinent replay:13 judge and32 application API tests passed; Compose config passed
with only the known unreadable Docker user-config warning. The earlier combined
test invocation emitted all assertion dots but did not return a final marker before
the polling boundary; separated reruns completed normally and supersede it. Real
Gemini behavior, quota/billing, runtime UI status, CI/deployment and inherited V3
dirty paths remain outside this review. Report:work/phase9/repo-review.md.

## Phase 10 bounded smoke attempt

The current process exposed neither `GEMINI_API_KEY` nor the required model value.
The one permitted invocation of `work/phase10/smoke_gemini.py` exited1 at its local
credential guard with `GEMINI_API_KEY unavailable`:0 HTTP calls,0 tokens, no temp
cache creation and no persistent application DB write. A read-only presence check
against the already-running `anidown-v4` container was attempted without printing
secrets or restarting it, but the managed environment denied Docker API pipe access;
this is not evidence about container credential presence. Per the no-retry bounded
scope, no model-list/control request was made. Phase10 remains incomplete and no next
micro-phase was started.

## 2026-09-16 continuation audit

Recovered all three planning ledgers plus Git status/diff. Phase9 remains closed,
the index remains empty, and Phase10 is still blocked only by absent
`GEMINI_API_KEY` and exact model configuration. The smoke was not repeated and no
secret source was inspected. A bounded search found no Phase11 or other independent
micro-phase already authorized by `task_plan.md`; remaining deferred topics require
new scope and include excluded live/production boundaries. No code changed and no
test was pertinent, so none was run. V3, Sonarr writes, downloads, autosave, Git
index/configuration, and existing worktree changes were untouched.

## 2026-09-16 credential-load audit

William's newly configured Windows User-scope Gemini variables are not inherited by
this already-running agent. Presence-only checks found no Process/current-User or
Machine values. Direct William-hive reads were denied through PowerShell Registry,
WMI was unavailable, and a final `reg.exe` query captured entirely in memory was
also denied. No value, raw registry output, log, or history was exposed. The planned
smoke was not invoked, so there were0 HTTP calls,0 tokens and no cache/DB writes.
No code changed; therefore no test was pertinent. Recovery requires a Codex host or
new session whose process environment already contains the two named variables.

## 2026-09-16 Phase 10 credentialed one-shot smoke

Re-read the three planning ledgers plus Git status, tracked diff/stat and cached
diff; branch remains `v4` with the inherited dirty worktree and an empty index.
Presence-only checks confirmed
a non-empty `GEMINI_API_KEY` and exact `GEMINI_MODEL=gemini-2.5-flash-lite` without
printing either value. Ran `work/phase10/smoke_gemini.py` exactly once. It used the
frozen Phase4 production snapshot and made one remote call for Chainsaw Man with a
two-candidate sanitized shortlist. Gemini returned HTTP400 `INVALID_ARGUMENT`; the
judge failed closed as `provider_error` / `remote_unavailable`, with null decision,
confidence, reason and token usage, 220.7ms measured request latency, and a cache
miss. The loop stopped immediately, so Sailor Moon Crystal, JoJo and Helluva Boss
were not called. No successful response existed, therefore the conditional
identical cache-hit check was not run. Nadia remained deterministic `proposed` with
`llm_called=false`.

The smoke exited0 as designed. Its temporary SQLite cache directory was removed;
no new Phase10 residue or persistent application DB timestamp change was observed.
No V3/Sonarr write, download, autosave, production mapping mutation, application DB
mutation, Git stage/config/reset/discard, container restart, or second remote call
occurred. Phase10 remains open because provider success and the conditional cache
hit are unverified; the existing one-shot/no-retry allowance has been consumed.

## 2026-09-16 Phase 10.1 local remediation checkpoint

Re-read planning and Git state, then compared the Gemini request with Google's
current official `generateContent` Schema reference and Gemini 2.5 structured-output
example. Root-cause hypothesis: AniDown placed JSON-Schema-only fields inside the
OpenAPI-style `generationConfig.responseSchema` object—`type` was a string/null
array and `additionalProperties` was present. Added fake-opener assertions for the
documented request shape. The focused test failed RED against the prior request,
then passed GREEN after changing only `src/v4/llm_judge.py`: scalar uppercase Schema
types, `nullable:true` on `candidate_id`, and no `additionalProperties`.

Local verification: the focused Gemini request test passed, followed by all13
`tests.v4.test_llm_judge` tests. No matcher, resolver, application, storage, smoke
selection, V3, Sonarr, download, autosave, production mapping or Git state behavior
was changed. The single newly authorized real smoke remains unused at this
checkpoint.

## 2026-09-16 Phase 10.1 single real smoke result

Presence-only checks confirmed the inherited key and exact
`GEMINI_MODEL=gemini-2.5-flash-lite`, then the newly authorized smoke ran exactly
once. It used only the frozen Phase4 snapshot and made one remote call for Chainsaw
Man's two-candidate sanitized shortlist. The corrected request no longer returned
the prior HTTP400; Gemini instead returned HTTP404 with sanitized category
`NOT_FOUND`. The judge failed closed as `provider_error` / `remote_unavailable` with
null decision, confidence, reason and token usage, 384.2ms measured latency, and a
cache miss. The smoke stopped immediately; no other ambiguous group was called and
no retry occurred. With no successful response, the conditional identical cache-hit
check could not run. Nadia remained deterministic `proposed` with
`llm_called=false`.

The smoke exited0 as designed and its temporary SQLite directory was removed; no
new Phase10 cache residue exists. No persistent application DB path was used. No
V3/Sonarr write, download, autosave, production mapping mutation, matcher/resolver
change, Git stage/config/reset/discard, or secret disclosure occurred. Phase10.1
stops at this sanitized remote failure as required; Phase10 is not closed.

## 2026-09-16 Phase 10.2 local model migration checkpoint

Official Google model documentation verified stable model code
`gemini-3.1-flash-lite` with structured-output support. Added focused assertions for
the provider default, configured-provider fallback and REST endpoint. The focused
test failed RED on the former2.5 default and passed GREEN after the model-only
migration. Updated the optional V4 provider/config default, isolated V4 Compose
default, Phase10 smoke exact-model guard, operator documentation and capability
link; no request schema or judge behavior changed.

Local verification passed all13 `tests.v4.test_llm_judge` tests and
`docker compose --profile v4-preview config --quiet`. Compose emitted only the
known access-denied warning for William's Docker user config. No matcher/resolver,
application/storage, V3/Sonarr, download, autosave, production mapping or Git index
behavior changed. The single Phase10.2 real-smoke allowance remains unused at this
checkpoint.

## 2026-09-16 Phase 10.2 successful bounded smoke

Verified `GEMINI_API_KEY` presence without reading or printing it, then launched
`work/phase10/smoke_gemini.py` once with `GEMINI_MODEL=gemini-3.1-flash-lite` set
only in that child process. The frozen Phase4 snapshot produced two eligible remote
groups and two local skips. Chainsaw Man returned HTTP200 with candidate
`ecb5dbdce2f9d9dd`, confidence0.90, 950 prompt +124 candidate =1074 total tokens,
and1482.3ms latency. Its one identical repeat was a cache hit in0.8ms with no
additional HTTP call. Sailor Moon Crystal was locally not judge-eligible. JoJo S2
returned HTTP200 with candidate `4cc45e8785216e15`, confidence0.90, 1808 prompt
+117 candidate =1925 total tokens, and1547.9ms latency. Helluva Boss was locally
not judge-eligible. Total remote calls:2; cache-hit verifications: exactly1. Nadia
remained deterministic `proposed` with `llm_called=false`.

Both model outputs remain suggestions only; no mapping or review state changed.
The temporary SQLite cache directory was removed and no new Phase10 residue exists;
no persistent application DB path was used. No V3/Sonarr write, download, autosave,
production mapping mutation, matcher/resolver change, secret disclosure, Git
stage/config/reset/discard, or remote retry occurred. Phase10 is now complete.

The next deferred production-integration boundary is controlled live-adapter and
write-path adoption: approved-only routing into downstream Sonarr/download flows,
sandbox verification, and explicit prevention of concurrent V3/V4 writers. It is
not started or authorized by Phase10.2.

## 2026-09-16 Phase 11 controlled downstream integration

Re-read the three planning ledgers and inspected Git status/diff with a command-local
safe-directory override; the inherited dirty worktree remains intact and the index
remains empty. No usable `graphify-out/graph.json` exists, so the required graph-first
check fell back to bounded symbol/source inspection. V3 was mapped read-only:
`Core.job()` obtains Sonarr wanted/missing data through `Processor`, mapping
automation attaches source URLs, and `Downloader` performs downloads/moves plus
Sonarr rescan/rename commands. No V3 file was changed.

Added `docs/v4/downstream-execution.md`, the approved-only execution coordinator,
durable once-per-snapshot ledger, exclusive per-target `v3`/`v4` writer claims, a
fake adapter and an atomic isolated-filesystem sandbox adapter. The immutable V4
snapshot supplies the selected human-approved plan, candidate URL and exact source
episode to Sonarr coordinates. State/current-target/revision/approval checks,
ownership acquisition and execution reservation occur in one SQLite transaction.
Adapter failures retain only a bounded category, append an audit event and leave the
mapping item unchanged. Terminal execution rows and audit events are SQL-immutable.
No production adapter or runtime/API execution route was added.

TDD evidence: the initial focused suite failed because `src.v4.execution` did not
exist. Seven requested behavior tests then passed; review added an eighth RED test
showing terminal execution rows could be rewritten, followed by a SQL-trigger fix
and 8/8 focused GREEN. Adjacent and full V4 module groups passed:32 application API,
43 dataset/matcher/Phase3,47 production/release/store/shadow, and30 frontend/judge,
for160 V4 tests total. The matcher, resolver and Gemini judge were not edited.

After local tests passed, `work/phase11/smoke_sandbox.py` ran once against a
disposable SQLite database and isolated temporary receipt directory with network
opening patched to fail. It approved only fixture target Nadia `182:1`, routed one
target/one segment, published exactly one completed sandbox receipt, reported
`production_effects=false`, preserved the approved item byte-for-byte at the decoded
record level, and removed the temporary DB/receipt directory on exit. No Sonarr
request, content download, autosave, production mapping/application DB mutation,
secret access, V3 writer, Git stage/config/reset/discard, or production effect
occurred.

Phase11 is closed only for fake/isolated adapter integration. Production cutover is
still blocked on an idempotent real adapter and crash reconciliation, a disposable
Sonarr/library parity exercise, an authenticated explicit execution surface,
operator ownership-transfer/rollback procedures, and proof that V3 is stopped or
participates in the same guard. A separate production-write/download authorization
is required.

## 2026-09-16 Phase 12 production-cutover preparation

Recovered after the reported power loss before changing Phase12 code. Re-read all
three ledgers, inspected inherited Git status/diff statistics/name status and the
empty index with command-local safe-directory configuration, hashed/read the five
Phase11 source/test/contract/smoke artifacts, and found no missing/zero-length or
partial artifact. The only focused recovery suite required—8 Phase11 downstream
tests—passed in8.613s. No cleanup/reset/recovery write was needed.

No existing `graphify-out/graph.json` was available. The graphify runtime is now
installed, but the bounded phase used direct source/symbol inspection rather than
creating a broad new repository graph. Mapped the V4 API/runtime/store boundary and
froze `docs/v4/cutover-preparation.md` before implementation.

TDD RED initially failed because `src.v4.cutover` did not exist. Implemented an
immutable persisted execution envelope, explicit reserve/reconcile operations,
lookup-before-submit recovery, fail-closed unknown reconciliation, exact optional
preclaimed ownership, atomic attested V3/V4 transfer/rollback, constructor-injected
bearer-authenticated operator execution, and a redirect-disabled loopback-only HTTP
test gateway with response bounds/schema validation. Default `create_app` and
runtime still register no execution control. A first GREEN attempt exposed tuple vs
JSON-list envelope drift after restart; reservation now canonicalizes before return
and persistence. Phase11+12 focused tests then passed14/14.

All current V4 tests passed:14 Phase11/12,32 application API,33 frontend/runtime/
review-store/judge,43 dataset/matcher/Phase3 and44 production/release/shadow =166.
No matcher, resolver, Gemini, V3, frontend or Compose behavior was edited.

After tests, `work/phase12/smoke_cutover.py` ran exactly once. A disposable database,
ephemeral bearer token, attested V3→V4 fixture claim transfer, preclaimed approved
Nadia plan, loopback fake HTTP service and isolated atomic receipt produced exactly
one submission and one completed attempt with `production_effects=false`. Owner and
bearer tokens were absent from audit, and the temporary DB/receipt/service were
removed/stopped. No production endpoint, credential, database, mapping, download,
Sonarr command, autosave or V3 process was accessed.

Added `docs/v4/production-cutover-runbook.md`. Phase12 closes preparation only; it
does not supply a real adapter, production-effect policy, deployment secret wiring,
real disposable Sonarr evidence, authenticated reconciliation route/CLI, observed
production V3-stop proof, backup/restore evidence or production authorization.

## 2026-09-16 Phase 13 all remaining pre-cutover implementation

Recovered the Phase12 state and preserved the inherited dirty Worktree with an empty
Git index; all Git inspection used a command-local safe-directory override. Phase13
changed only V4 source/tests/docs and planning ledgers. It did not edit V3, frontend,
Compose, Gemini, matching or resolver code.

Added a dormant production-shaped adapter with explicit isolated/production permits,
bounded Sonarr v3 status/series/rescan requests, exact AnimeWorld episode selection,
allowlisted staging/library containment, explicit secret-file loading and a durable
execution-key journal. The journal records `preparing`, `dispatching` and `completed`;
any uncertainty after external effects begin remains an open reservation and cannot
resubmit automatically. Production-effect receipts remain rejected by default and
require an exact separately constructed authorization manifest. No default factory,
runtime, Compose or UI imports or constructs the adapter.

Added separately injected authenticated reconciliation/status controls, verified
SQLite online backup/integrity/restore-to-new-path operations, sanitized execution
health, fail-closed readiness, and one-envelope canary preflight. Updated the runbook
and added `docs/v4/phase13-precutover.md` with threat model, evidence and stop gates.

TDD began with an import failure for the missing production authorization/module.
Seven focused Phase13 regressions pass. The combined Phase11–13 execution/cutover set
passes21/21, and all173 V4 tests pass in67.201s after final one-execution authorization
binding. Bytecode compilation of all changed
V4 modules also passes. The isolated integration used only a temporary SQLite DB,
temporary staging/library/journal roots, a fake downloader and an in-process loopback
Sonarr double; it verified bounded status/series calls, `X-Api-Key` header handling,
rescan command shape, idempotent lookup, secret absence and unknown-outcome blocking.
All temporary test artifacts were cleaned. The first backup-test failure initially
left one disposable backup; its exact path/size were verified under `work` and only
that artifact and its now-empty directory were removed.

Docker client29.3.1 is installed, but the current account received access denied for
the Docker config and permission denied on the engine named pipe. No image/container
operation or network fallback was attempted. Consequently Phase13 claims no real
disposable-Sonarr compatibility result. No production endpoint, credential, database,
library, download, Sonarr write, autosave, mapping mutation, Git stage/config/reset/
discard, or current-system enablement occurred.

Phase13 completes the pre-cutover software implementation only. Remaining evidence is
a reviewed disposable real-Sonarr/test-library restart exercise, production backup and
restore rehearsal, reviewed secret/path/origin deployment input, observed V3 stop or
shared-guard quiescence, and a separately authorized single production canary.

## 2026-09-16 Phase 14 final pre-cutover evidence started

Resolved the planning workflow to the legacy root files, re-read all ledgers and
inventoried the complete dirty Worktree/index without Git mutation. The staged delta
remains empty. Froze a bounded Worktree review basis at HEAD `e979b64c...` with
size/SHA-256 for 16 Phase11–13 code/test/doc/smoke files. Read-only Standards/Spec review found one
P1 pre-effect authorization ordering defect, recorded in `findings.md`; no production
or external service was accessed. Review basis is now closed and implementation will
use RED/GREEN fake-only tests.

Phase 14 RED/GREEN: a focused fake-adapter test reproduced the P1 defect by observing
one adapter call and a consumed execution row before authorization rejection. The
coordinator now compares the supplied and adapter-required capabilities against the
deterministic execution key before reservation and again before dispatch; the real
adapter independently enforces its permit before journal creation. Denial and exact
matching-capability paths pass locally. A second RED test showed backup evidence lacked
a logical fingerprint; backup/verify/restore now require equal logical SHA-256 and the
focused restore test passes. No external service or non-temporary path was used.

Phase 14 evidence checkpoint: the expanded pre-cutover module passes 10/10, including
pre-reservation authorization denial, exact capability success, corrupt/incomplete
journal fail-closed behavior, post-effect unknown no-resubmit, logical backup restore,
bound canary evidence, secret-free receipts and default-unregistered recovery control.
The combined Phase11–14 downstream/cutover gate passes 24/24. All dependencies are
temporary files, fake downloaders or in-process loopback HTTP only.

Phase 14 static-check attempt: changed V4 modules compiled, but global `python -m pip
check` stopped the combined command on an unrelated host-environment conflict:
`hf-gradio 0.3.0` requires `gradio-client>=2,<3` while the host has 1.14.0. AniDown's
requirements do not declare either package, no dependency was changed, and no install/
upgrade/remediation was attempted. Project dependency consistency is therefore Not
verified by the global environment; remaining static checks will run separately.

Phase 14 final local verification checkpoint: Compose rendering passed with an empty
temporary Docker config and no engine access; default runtime/API/Compose/frontend
contain zero production-adapter registration references; count-only scans over the
Phase 14 security scope found zero private-key/Gemini/assigned long-secret signatures.
The temporary Docker config was removed. All 176 V4 tests pass in 71.101s. No tests
were skipped and no production/external endpoint, secret, DB, path or process was used.

## 2026-09-16 Phase 14 complete

Final bounded Standards/Spec review on the corrected basis found no open P0–P3
findings. The initial P1 pre-effect authorization defect is closed by checks before
reservation, before dispatch and independently before adapter journal/effects. Backup
restore now compares a logical SQLite fingerprint; canary preflight requires one valid
envelope and its exact authorization; corrupt/incomplete journals remain unknown and
never dispatch.

Retained `docs/v4/phase14-evidence.md` and the sanitized 16-file manifest at
`work/phase14/evidence-manifest.json`. Manifest verification found zero missing or
mismatched files, the aggregate SHA-256 matched, and the staged delta remained empty.
The runbook and Phase 13 evidence now describe the corrected gate. No production
endpoint, secret, process, database, path, container, download, Sonarr command, V3
change, runtime/Compose/UI enablement, autosave, mapping mutation or Git mutation
occurred.

Phase 14 closes local pre-cutover implementation/evidence only. Separate operational
authority and evidence are still required for a production-equivalent disposable
Sonarr/restart exercise, independent final review, real database/library backup and
restore rehearsal, reviewed secret/path/origin deployment, observed V3 quiescence and
ownership transfer, and one explicitly authorized production canary.

## 2026-09-16 Phase 15 started

Recovered the legacy root plan (no selector or `.planning` directory), all three
ledgers and the complete dirty Worktree without mutation. The staged delta remains
empty. Selected the next implementable non-production gate: a dormant, loopback-only,
explicitly marked disposable-Sonarr validation harness using isolated paths and a
fixture artifact source. This phase will not invoke Docker, any current service or
production resource, and will not add runtime/API/UI/Compose registration.

Phase 15 RED: added three focused contracts for exact confirmation/marker/loopback/
path containment, one fixture-artifact rescan with fresh-adapter restart lookup and
sanitized evidence, and version mismatch before any series/journal/artifact effect.
The focused module fails at import because `src.v4.disposable_validation` does not
exist, confirming the new behavior is not already implemented.

Phase 15 GREEN: implemented `src/v4/disposable_validation.py` and an unregistered
CLI wrapper under `work/phase15`. Configuration requires an exact manifest and
confirmation, literal marker, loopback HTTP origin, bounded exact version, contained
distinct nonsymlink paths and contained API-key file. The runner refuses nonempty
effect roots, creates one fixed fixture artifact, issues one bounded rescan, then
constructs a fresh adapter and proves completed lookup from the durable journal.
Evidence excludes the key, origin and filesystem paths. All 3 focused tests pass.

The first combined Phase 11–15 test command was interrupted by the user's new
corrective-phase instruction before it returned evidence; it is not counted as a
pass or failure and will not be repeated unchanged until the corrective code lands.

## 2026-09-16 Phase 15.1 corrective phase started

Re-read all three ledgers plus Git status, tracked diff/stat and cached diff. The
inherited dirty state is preserved and the staged delta remains empty. Security
review found a reachable harness-only gap: a filesystem marker and exact version do
not establish that the loopback Sonarr service is disposable. The correction will
require the status response's fixed disposable instance identity before any series
lookup, journal, artifact or rescan. No service will be invoked outside the in-process
test double.

Phase 15.1 RED/GREEN: the same-version `Current Sonarr` loopback regression failed
because no exception was raised and the old runner proceeded through its effect path.
The runner now requires status `instanceName` to equal a fixed disposable identity
before any series lookup or local journal. Phase 7.1 corrected that literal to the
real-Sonarr-compatible `Sonarr AniDown V4 Disposable Validation`; the prior literal
could not be saved because Sonarr requires its name as the first or last word.
All 4 focused tests pass, including zero series calls, commands, journal files and
library artifacts for wrong identity or version.

An additional RED evidence-contract test rejected the misleading `restart_lookup`
label: reconstructing the adapter and reading the journal does not restart Sonarr.
The sanitized result now reports only `fresh_adapter_lookup`. The Phase 11–15
execution/cutover set passes 28/28; the final full V4 suite passes 180/180 in 72.946s
with no skips. Bytecode compilation passes for the new module and wrapper. A missing-
manifest CLI check exits 1 with only `invalid_validation_config`, and default runtime/
API/Compose/frontend contain zero registration references.

Added `docs/v4/disposable-sonarr-validation.md` and updated the cutover runbook. No
real Sonarr, Docker engine, current service, production endpoint, secret, database,
path, download, library, command, V3 mapping, qBittorrent, Plex, nginx, runtime, Git
index or Git configuration was read or mutated. Only temporary test directories and
in-process loopback HTTP doubles were used. One combined ledger patch initially
missed its expected context and made no change; it was reapplied against the verified
file tail.

## 2026-09-17 Phase 16 started

Recovered `progress.md`, `task_plan.md`, `findings.md`, complete Git status, tracked
diff/stat and cached diff before action. The legacy root plan remains selected, the
staged delta is empty and all inherited dirty paths are preserved. Phase 16 is limited
to frozen/captured inputs, temporary evaluation output and V4 source/tests/docs. It
will not query or mutate current Sonarr, V3, qBittorrent, Plex, nginx, libraries,
downloads, production DB/config/secrets/runtime, Git index/config or other services.

The correction loop will keep distinct terminal buckets for filtered untagged items,
waiting/airing, unavailable/no-source, true review and safe proposal; metrics may not
be improved by relaxing title/year/episode/language or shortlist-membership gates.
## Phase 16 corrective loop (active)

- Recovered the inherited dirty worktree read-only; staged delta remains empty and no inherited changes were reset, cleaned, staged, committed, or discarded.
- Built a local relationship graph for `src/v4` and traced snapshot creation through retrieval, deterministic matching, release resolution, and application enrichment. Graph generation reported duplicate path-form nodes, so it is navigation evidence rather than an integrity oracle.
- First concrete defects confirmed: snapshot admission lacks the `animeworld` tag gate, and retrieval still injects V3-observed URLs after native lexical ranking.
- Independent frozen-corpus audit is running read-only while focused RED tests are prepared locally.
- Focused RED was captured for tag admission, native alias retrieval without V3 URL injection, AoT multipart composition, AUTO DUB selection, and reason-code deduplication. All five are now GREEN.
- Added a versioned, reviewed title-equivalence data path with the first conservative Grendizer/Goldrake set. Alias evidence affects title identity/retrieval only and cannot create season or episode proof.
- Multipart auto-composition now requires sequential explicit `Part`/`Parte` labels, complete contiguous reset numbering, exact/scoped first-release identity, and a matching Sonarr air-date anchor at every boundary.
